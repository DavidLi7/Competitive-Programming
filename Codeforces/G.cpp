#include <iostream>
#include <vector>
#include <math.h>
#include <algorithm>

using namespace std;

const int MAXN = 1e6 + 5;

int N, a[MAXN];

int main() {
     
    ios_base::sync_with_stdio(0);
    cin.tie (0);
    cout.tie (0);

    cin >> N;
    
    
}
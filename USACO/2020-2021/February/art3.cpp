#include <iostream>

using namespace std;

const int MAXN = 305;

int N, a[MAXN];

int main() {
     
    ios_base::sync_with_stdio(0);
    cin.tie (0);
    cout.tie (0);

    cin >> N;
    for(int i = 0; i < N; i++){
        cin >> a[i];
    }

}
#include <iostream>
#include <algorithm>

using namespace std;

const int MAXN = 305;

int N, K;
long long A[MAXN], sub[MAXN][MAXN], add[MAXN][MAXN];

int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);

    cin >> N >> K;
    for(int i = 0; i < N; i++){
        cin >> A[i];
    }

}